{
  "name": "kitsune-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Kitsune 0G Verifiable Agent Marketplace",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:galileo": "hardhat run scripts/deploy.js --network galileo",
    "deploy:aristotle": "hardhat run scripts/deploy.js --network aristotle"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.2"
  }
}
